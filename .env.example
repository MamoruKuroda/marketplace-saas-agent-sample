# Copy to `.env` and set a strong password locally. `.env` is git-ignored.
# Requirements: at least 8 characters, mixing three of upper / lower / digit / symbol.
MSSQL_SA_PASSWORD=ChangeMe_Local_Dev_1
